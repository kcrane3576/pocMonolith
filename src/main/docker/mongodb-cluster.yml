version: '2'
services:
    pocmonolith-mongodb:
        image: mongo:3.4.8
        ports:
            - "27017:27017"
        command: mongos --configdb pocmonolith-mongodb-config
    pocmonolith-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    pocmonolith-mongodb-config:
        image: mongo:3.4.8
        container_name: pocmonolith-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
